<h2>All Events</h2>

<% @events.each do |event| %>
    <p>
        Event: <a href="/events/<%= event.id %>"><%= "#{event.id}. #{event.title}" %></a> 
        <br>
        Host: <%= "#{event.user.id}. #{event.user.name}" %>
    </p>
<% end %>